import { NiebieskiTekstDirective } from './niebieski-tekst.directive';

describe('NiebieskiTekstDirective', () => {
  it('should create an instance', () => {
    const directive = new NiebieskiTekstDirective();
    expect(directive).toBeTruthy();
  });
});
